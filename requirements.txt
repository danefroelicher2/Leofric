# Leofric — Python dependencies.
#
# Installed incrementally per phase to avoid long builds before they are needed
# (face_recognition/dlib in particular takes a long time to compile on the Pi).
# Uncomment each group at the start of its phase.

# --- Foundation (Phase 1B, install now) ---
python-dotenv==1.0.1

# --- Vision (Phase 1C-1E) ---
# Headless build: no GUI/X11 dependencies, correct for a headless Pi where we
# save frames to disk rather than call cv2.imshow(). Smaller and fewer system libs.
# Pinned to the 4.x line: OpenCV 5 removed HOGDescriptor (our person detector)
# from the Python bindings, and 4.x is the version the wider CV ecosystem targets.
opencv-python-headless>=4.9,<5
numpy

# --- Identity (Phase 1F) ---
# face_recognition

# --- Audio (Phase 1H-1I) ---
# pyaudio
# pvporcupine
# openai-whisper

# --- Brain + storage (Phase 1G, 1J) ---
# requests
# supabase
